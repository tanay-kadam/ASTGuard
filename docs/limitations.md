# Limitations

See `IMPLEMENTATION_NOTES.md`. Most importantly, this local pass did not run the
research environment, official data, neural models, or external baselines. The
current DFG front-end is narrower than the registered protocol and blocks a
scientific release until completed and independently validated.

