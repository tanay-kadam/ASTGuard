# Reproducibility

Runs are immutable directories keyed by timestamp, config hash, and seed.
Configs, source identity, package/hardware snapshots, split/cache hashes,
checkpoints, predictions, thresholds, metrics, and statuses are retained.

