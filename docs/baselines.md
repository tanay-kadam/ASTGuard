# Baselines

Prior, matched lexical TF-IDF, controlled CodeBERT variants, GraphCodeBERT C/C++
adaptation, LineVul, and ReGVD are the registered suite. Unverified external
implementations receive a status row and never a dummy metric.

